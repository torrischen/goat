package anthropic

// Anthropic API and stream errors are returned by the official SDK unchanged.
// This file intentionally remains as a package marker for provider-specific
// error behavior if the neutral interface grows error classification later.
